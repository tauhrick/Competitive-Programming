#include <bits/stdc++.h>

#ifdef LOCAL
#include "../testlib/testlib.h"
#else
#include "testlib.h"
#endif

using namespace std;

int main(int argc, char * argv[]) {
  registerValidation(argc, argv);

  const int kLimN = int(1e9);
  int n = inf.readInt(1, kLimN, "n");
  inf.readSpace();

  const int kLimK = 10;
  int q = inf.readInt(1, kLimK, "k");
  inf.readEoln();

  inf.readEof();
  
  return 0;
}